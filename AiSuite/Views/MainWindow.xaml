<Window
    x:Class="AiSuite.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:tools="clr-namespace:AiSuite.Views.Tools"
    xmlns:viewModels="clr-namespace:AiSuite.ViewModels"
    xmlns:viewModels1="clr-namespace:AiSuite.ViewModels.Tools"
    Title="{Binding Title}"
    Width="525"
    Height="350"
    d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModels1:RectMeasureViewModel}">
            <tools:RectMeasure />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels1:PromptBatcherViewModel}">
            <tools:PromptBatcher />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels1:ModelBrowserViewModel}">
            <tools:ModelBrowser />
        </DataTemplate>
    </Window.Resources>

    <Grid Background="{StaticResource DarkBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            Grid.Column="0"
            Margin="4"
            Background="{StaticResource MiddleBackgroundBrush}"
            BorderThickness="0,0,1,0"
            CornerRadius="5">
            <ListBox
                x:Name="ToolListBox"
                BorderThickness="0"
                ItemsSource="{Binding Tools}"
                SelectedItem="{Binding SelectedTool}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:IToolViewModel}">
                        <TextBlock
                            Padding="10,5"
                            FontSize="{StaticResource CommonFontSize}"
                            Foreground="{StaticResource CommonForegroundBrush}"
                            Text="{Binding DisplayName}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="ListBoxItem">
                        <Setter Property="Margin" Value="4" />
                        <Setter Property="Template">
                            <Setter.Value>

                                <ControlTemplate TargetType="ListBoxItem">
                                    <!-- Border の代わりに Card を使う -->
                                    <materialDesign:Card
                                        x:Name="TemplateRoot"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        ClipToBounds="True"
                                        UniformCornerRadius="8">

                                        <materialDesign:Ripple
                                            Padding="{TemplateBinding Padding}"
                                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                            Feedback="LightSkyBlue">

                                            <ContentPresenter />
                                        </materialDesign:Ripple>
                                    </materialDesign:Card>

                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="TemplateRoot" Property="Background" Value="{StaticResource SelectionBrush}" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="TemplateRoot" Property="Background" Value="{StaticResource HoverBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>

        <ContentControl Grid.Column="1" Content="{Binding SelectedTool}" />
    </Grid>
</Window>